# 安装器横向星空背景

生成方式：内置 image_gen（不是 API/CLI 回退）。
风格参考：仓库已有 `Resources/LoginCosmos-v2.png`。
用途：安装器背景，不写入客户端增量包，不替换应用原有品牌素材。
输出文件：`cosmos-installer-v1.png`，已生成并检查留白、比例与配色后加入。

## 最终生成提示词

Use case: stylized-concept. Asset type: production background bitmap for TokenPro's Windows installer, not a screenshot or UI mockup. Image 1 is the existing TokenPro brand style reference. Create one landscape cosmic starfield background, approximately 497:360 aspect ratio (1392x1008 preferred), using the reference's deep midnight navy, restrained electric blue and violet nebula filaments and fine stars. Keep the central and upper 75 percent exceptionally dark and quiet so native white Chinese wizard text will stay legible. Concentrate a graceful thin luminous nebula arc along the lower edge and subtle side edges. Polished, understated AI platform brand mood, rich space depth, no excessive bloom. Keep reference palette and realistic nebula texture; adapt composition for a horizontal installer without stretching the reference. No text, no letters, no logo, no buttons, no frame, no interface elements, no watermark, no planet or other foreground objects. Only the reusable background image.
