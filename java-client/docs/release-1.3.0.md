# TokenPro v1.3.0

- Codex 文本和生图模型均可多选；每轮请求使用 Codex 中选中的模型和对应分组。
- 配置采用 native-v2，取消 provider 级固定图片模型。全局 Key 保持不变。
- 后端 R56 根据认证凭据与 Turn ID 关联 Images 请求；跨会话、同名模型不同分组隔离。
- 映射缺失、过期或冲突明确失败，不自动切换模型。旧 native-v1 客户端继续兼容。
- 本地不安装图片桥接服务，不生成桥接配置。官方切换、会话修复和圆角窗口逻辑保留。

发布顺序：后端 R56 健康并通过真实链路验收后，再发布 v1.3.0 客户端。
