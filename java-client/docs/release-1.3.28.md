# TokenPro v1.3.28

- Claude Desktop 的 TokenPro 模型现在会声明完整的 Effort 能力，显示从 Faster 到 Smarter 的 Effort 滑杆。
- 自定义模型路由继续使用原有 ID，不伪装为 Claude 原生模型，也不修改历史对话。
- 配置校验会拒绝缺少 Effort 能力声明的模型列表，避免界面静默退回 Low。

验证：534 项自检通过。
