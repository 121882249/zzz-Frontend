# TokenPro v1.3.16

- 桌面端与 CLI 改用后端专用全局 Key 接口，不再依赖 API Key 列表的分页与排序。
- 仅在旧后端返回 404 时兼容原分页查找；接口故障不会被静默降级掩盖。
- 严格校验全局 Key 的类型、状态、编号和完整凭据，禁止普通分组 Key 被误用为系统凭据。
- 配合后端按请求验证模型与分组；重置全局 Key 后重新连接即可事务性写入新凭据。
