# Claude 桌面版模型切换

用户最终要求：只用一把 Key，在 Claude 桌面内部切换已选模型，不因切换模型退出应用。不修改云端后端代码，不开发命令行入口。

- 唯一专用 Key 为 `TokenPro · Claude`。不存在按分组创建多把 Key 的产品逻辑。
- 本地服务内置在 TokenPro 的同一个可执行文件里，只监听 `127.0.0.1`，使用独立随机本地令牌。
- 每个请求用所选模型匹配分组，持有跨进程文件锁，检查当前账户和 Key，必要时通过已有账户 API 修改 `group_id` 并重新读取确认。
- 锁持续到上游回答结束或请求取消，包括整个流式回答。来自其他分组的请求排队，不能在流式回答中途换组。
- 云端推理目标固定为 `https://tokenpro.work`。本地端口不是另一个服务商；原 CC Switch 配置的 `api.teamorouter.cn` 不用于 TokenPro 转发。
- GPT 分组转换 Messages 与 Responses；Grok、Claude、Gemini 分组使用现有 Messages 兼容接口。不静默回退到其他模型。
- Claude Desktop 对模型名有厂商筛选，配置使用不含厂商误导信息的本地路由 ID，`labelOverride` 显示真实的所选模型名称。路由 ID 不证明上游供应商的实际模型身份。
- 初次接入和改动可选列表后需要重新加载配置一次；加载之后，模型切换不重启 Claude。TokenPro 的“打开应用”在配置未变化时仅打开/唤起已有进程。
- 恢复官方配置保留已有登录、聊天和其他配置档，撤销本地连接。

## 当前验证进度

- 四个所选名称已实际显示在 Claude Desktop 的模型菜单中。
- 单 Key 顺序切换四个分组的流式接口测试均有文本和结束事件。
- GPT、Grok、Claude 的工具调用和工具结果往返通过。
- Gemini 工具调用返回正常，但工具结果续接仍需进一步验证；一次续接再次返回工具调用，不能据此声称全通过。
- 最小请求通过 Claude Desktop 内置引擎验证 GPT，返回 OK。
- 完整桌面对话触发过额外消息角色兼容问题，已增加诊断；Mac 锁屏中，尚待完整桌面复测。

## 数据处理

凭据文件目录权限 0700、文件权限 0600，不使用钥匙串。诊断只记录模型名称、状态、角色类型和错误，不记录消息正文、工具参数或 Key。恢复官方配置不会删除现有其他服务商配置档。
